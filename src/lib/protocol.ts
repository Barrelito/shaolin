// Full protocol data split into per-level files under ./protocol/
// This file re-exports everything for backward compatibility.

export { LEVELS, getLevelByNumber, getBlocksForLevel } from "./protocol/index";
